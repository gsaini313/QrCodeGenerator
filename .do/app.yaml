name: qr-code-generator

services:
  - name: web
    image:
      registry: ghcr.io
      repository: gsaini313/qr-code-generator
      tag: latest
    environment_slug: custom  # Change from 'docker' to 'custom'
    github:
      branch: main
    routes:
      - path: /
